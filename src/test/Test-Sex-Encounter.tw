:: Test-Sex-Encounter [nobr]

<<if !$ui || !$ui.npc>>
	ERROR: ui.npc not set
	<<return>>
<</if>>
<<set _npc = $ui.npc>>
<<set _n = _npc>>
/* This is to be set to null at the end of each encounter.*/
<<set $n = _n>>

<<run setup.normalizeBodyparts($mc)>>
<<run setup.normalizeBodyparts(_n)>>

<<if !$mc.bodyparts && $mc.body>>
	<<set $mc.bodyparts = Object.keys($mc.body)>>
<</if>>
<<set $gamelocation = "Your Room">>
/* ensure room exists BEFORE UI */
<<if !$room>>
<<set $room = {
  type: "bedroom",
  location: "large floor",
  furnitureFlags: setup.getRoomFurnitureFlags("bedroom")
}>>
<</if>>

/* ensure sex exists */
<<SexMemory>>
/* local convenience copy */
<<set _sexCount = $npcMemory[_key].sexCount>>

<<set $sex.position = "Standing">>
<<set $sex.subject = null>>
<<set $sex.object = null>>
<<set $sex.engagedParts= { subject: [], object: [] }>>
<<set $sex.penetration = null>>
<<set $sex.phase = "tease">>
<<set $sex.history= []>>
<<set $sex.action= "Waiting">>


<<if $sex.roles.dom.name == $mc.name>>
  <<set $sex.role = "top">>
  <<run setup.updateSubjectObject($sex)>>
<<else>>
  <<set $sex.role = "bottom">>
  <<run setup.updateSubjectObject($sex)>>
<</if>>

/* ALWAYS rebind subject/object to current */
/*<<set $sex.subject = $mc>>
<<set $sex.object  = _n>> */



/* keep furniture flags synced */
<<set $room.furnitureFlags = $room.furnitureFlags || setup.getRoomFurnitureFlags($room.type)>>
<<set $sex.furnitureFlags  = $room.furnitureFlags>>

/* Debug checking to confirm things exist */
/*MC parts: <<print $mc.bodyparts.join(", ")>><br>
NPC parts: <<print _n.bodyparts.join(", ")>><br> 
Sex Subject: <<print $sex.subject.name>><br>
Sex Object: <<print $sex.object.name>><br>
<<button "Inspect">>
	<<WithNPC _n>>
	<<run Dialog
		.create("NPC: " + $ui.npc.name)
		.wikiPassage("UI-NPC-Inspect")
		.open()
	>>
<</button>>
<br> */


/*Arousal meter for both $mc and _n */
<<set _n.arousal = _n.arousal or 0>>
<<set $mc.arousal = $mc.arousal or 0>>

/* Arousal bar*/
<div id="arousal-ui">
  <<ArousalUI>>
</div>
<br>
<div id= "sex-container">
<<RoomRelocationUI>>
<<SexEncounterUI>>
<<button "End Session">>
			        <<set $lastNpc = _n>>      <!-- remember who you were with -->
			        <<set $sex = null >>  <!-- optional reset -->
			        <<goto "Your Room">> <!-- or "Your Room" or whatever -->
<</button>>
</div> 

