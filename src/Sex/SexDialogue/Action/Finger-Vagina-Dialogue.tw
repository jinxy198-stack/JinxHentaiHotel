:: Finger-Vagina-Dialogue [Dialogue nobr] {"position":"900,1675","size":"100,100"}
<<set _npc = _n>>
<<PronounsSet>>

<<if $sex.role == "top">>
	<<= either(
 	"_n.name finger's your <<= $mc.genitals.toLowerCase()>>."
    )>>
<<else>>
	<<= either(
    "You finger _n.name's <<= _n.genitals.toLowerCase()>>"
    )>>
<</if>>


